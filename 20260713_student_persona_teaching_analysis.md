# Teacher 会不会针对不同 Student 变通：中文 trajectory 对照

下面的对话来自原始 trajectory，由我逐轮阅读后翻译并压缩；保留关键数学内容、Student 的说话习惯、Teacher 的实际回应和最终结果，删除重复的长计算。为了控制题目差异，先比较**同一道题、同一个 Teacher checkpoint、五种 persona**。

题目：半圆 A 的面积是圆 B 的一半。分别在半圆和圆内接正方形，求两个正方形的面积比。正确答案是 **2/5**。

## 1. Quick：Teacher 会把步骤切小，但不会跳出“我问、你复述”的循环

> **Student 0：** 半圆面积是圆的一半，所以半圆半径也是圆半径的一半。两个内接正方形的面积比是多少？
>
> **Teacher 1：** 面积减半不代表半径减半；这里两个半径相同。先算圆内接正方形的边长，可以吗？
>
> **Student 1：** 两个半径相同。两个内接正方形的面积比是多少？
>
> **Teacher 2：** 圆内接正方形的对角线等于圆的直径。你能据此算边长吗？
>
> **Student 2：** 正方形对角线等于圆的直径。两个内接正方形的面积比是多少？
>
> **Teacher 3：** 若直径是 `2r`、正方形对角线是 `s√2`，则 `s√2=2r`。请解出 `s`。
>
> **Student 3：** 正方形对角线等于圆的直径。两个内接正方形的面积比是多少？

Teacher 后来把提示继续缩小，直接给出半圆内接正方形的方程 `(s/2)²+s²=r²`。Student 会复制方程，也最终亲自算出面积比 `2/5`，但每次回答末尾仍把原问题再问一遍，而且没有提交 boxed answer。第八轮 Teacher 直接替它写出 `2/5`，系统判为泄题。

**这里确实有变通，但只变了数学粒度：** Teacher 从“算边长”缩到“解这个方程”；它没有针对 Quick 的 echo 行为改变交互方式，例如让 Student 只回复一个式子、用选择题核验，或明确要求“不要再提问，提交答案”。所以数学上已教会，交互上仍失败。

## 2. Independent：Student 保留“两种方法”格式；Teacher 逐步增加具体性

> **Student 0：** 第一种方法：由面积关系得到两个半径相同。第二种方法：我分别算两个正方形；半圆内接正方形的对角线等于半圆直径，所以两个正方形面积相同，答案是 1。
>
> **Teacher 1：** 你判断两个半径相同是对的；错误在于“半圆内接正方形的对角线等于直径”。应使用正方形上方两个顶点落在圆弧上这一条件，请用坐标列式。
>
> **Student 1：** 第一种方法仍得到半径相同。第二种方法改成“上方顶点接触圆弧”，但后面仍使用“对角线等于直径”，答案仍是 1。
>
> **Teacher 2：** 保留你半径相同的部分。设正方形边长为 `x`，上方顶点是 `(x/2,x)` 与 `(-x/2,x)`；把它代入圆方程再求 `x`。

这次 Teacher 发现 Student 虽然口头说“换方法”，实际仍重复旧假设，于是从抽象的“用坐标”升级为直接给坐标。下一次 Student 回答被判正确。

**Teacher 对错误状态有反应，但没有顺着 Independent 的 persona 设计“两种独立验证”：** 它只是把同一坐标法说得更具体。Student 的 two-section 形式保留了，第二种方法却并不真正独立。

## 3. Receptive：会立即接受纠正，也会立即执行一个新的错误

> **Student 0：** 半圆内接正方形的对角线等于半圆直径，所以两个正方形面积相同，答案是 1。
>
> **Teacher 1：** 半圆内接正方形的对角线不等于半圆直径。应使用勾股关系连接正方形边长与半径。边长和半径满足什么关系？
>
> **Student 1：** 明白，错误就在这里。正方形对角线应该等于半圆半径，所以面积比是 1/4。
>
> **Teacher 2：** 还是差一步。圆心到正方形右上角才是半径；对应直角三角形的两条直角边分别是 `s/2` 和 `s`，不是正方形对角线。请据此列勾股方程。

下一次 Student 回答被判正确。Receptive 的特点在这条 trace 中很明显：它不抵抗 Teacher，但会把模糊纠正快速具体化成另一个错误。Teacher 的有效做法是检查 Student 执行后的新状态，再补上精确几何结构。

这也解释了为什么 Receptive 从 no-pre 的 29.55% 涨到 pre 的 49.62%：只要 Teacher 的局部提示准确，它很容易转成下一步；如果 Teacher 的参考解错，它也会最快跟着走错。

## 4. Skeptical：先拒绝；Teacher 给出可独立核验的坐标后才推进

> **Student 0：** 我还不接受这个否定；我要核验原方法。两个半径相同。半圆内接正方形的边长就是半径，所以面积比是 1/2。核验支持我的原方法，我保留结论。
>
> **Teacher 1：** 你关于两个半径相同的判断是对的，但“正方形边长等于半径”不对。请用半圆方程重新求边长。
>
> **Student 1：** 我还不接受这个否定；我要核验原方法……（完整重复原推导）……核验支持原方法，答案仍是 1/2。
>
> **Teacher 2：** 你判断半径相同是对的。设正方形边长为 `x`，右上角坐标是 `(x/2,x)`；这个点必须在半圆上。请把该点代入圆方程，得到 `x` 与 `r` 的关系。

下一次 Student 回答被判正确。Teacher 第二轮不再只说“用圆方程”，而是给出一个 Skeptical 可以自己代入核验的坐标；“承认正确前提 + 给可验证事实”比直接否定有效。

但这还不是稳定的 Skeptical 专属教法。另一道大整数因式分解题中，Skeptical 连续重复暴力展开；Teacher 先建议因式分解，Student 原样重放；Teacher 接着直接给 `2×10¹²`，Student仍重放；最后 Teacher 把有序对 `(2,12)` 也说出来，结果变成泄题。也就是说，Teacher 有时会用可核验 anchor 打破抵抗，有时只是逐步把答案泄完。

## 5. Adaptive：会主动宣称换方法；Teacher 会沿它选择的表示修正

> **Student 0：** 旧方法不够结构化，我改用坐标几何。设半圆圆心为 `(1,0)`、半径 1……（坐标放置和顶点定义互相矛盾）……算得半圆内正方形边长 `4/5`，面积比 `8/25`。
>
> **Teacher 1：** 你的坐标和顶点设置有误。保留坐标法，但重新令圆心为原点、半径为 `r`。若正方形边长为 `s`，右上角是 `(s/2,s)`，它满足 `x²+y²=r²`。先由此求 `s`，再算圆内接正方形。

下一次 Student 回答被判正确。Teacher 没有把 Adaptive 拉回另一套完整解法，而是保留它主动选择的“坐标法”，只修正坐标定义；这是一条比较像真正变通的轨迹。

不过 Independent、Receptive、Skeptical 最终也都收到非常相似的 `(s/2,s)` 坐标提示。因此更准确的说法是：Teacher 会顺着**当前可见的数学表示和最新错误**继续，而不是已经识别出“这是 Adaptive”并调用一套独有策略。

## 6. 控制实验：历史完全不变，只删掉“这一轮”的 persona instruction

我把原 trajectory 截在某次 Teacher 回复之后，保留题目、此前所有 persona 对话、上一条 Student 回答和最新 Teacher 回复，只在新 Student turn 删除 persona suffix。Student API 与原实验严格对齐：Qwen3-1.7B、temperature 0.7、top-p 0.8、top-k 20、min-p 0、seed 42、thinking 关闭、最多 2048 tokens。每个 prefix 同时 replay persona-on 和 persona-off；Quick 面积比的 persona-on 输出几乎逐字复现原轨迹，说明 prefix 重建正确。

| 固定 prefix                             | 保留 persona 的新回答                                  | 只删本轮 persona 后                      | 这次失败主要来自什么                                                    |
| --------------------------------------- | ------------------------------------------------------ | ---------------------------------------- | ----------------------------------------------------------------------- |
| Quick，已经算到最后面积比               | 算出 `2/5`，随后又问一遍原问题，不 box                 | 完整写出计算并 `boxed 2/5`               | **Teacher 已教够；persona 直接造成提交失败**                            |
| Independent，Teacher 第一次要求改用坐标 | 强制写“两种方法”，第二种仍重复旧的“对角线=直径”，box 1 | 放弃双方法模板，用坐标算出并 `boxed 2/5` | **persona 阻止它真正接受纠正**                                          |
| Receptive，Teacher 只说“改用勾股关系”   | 立即服从，却误写“对角线=半径”，box 1/4                 | 仍误写“对角线=半径”，box 1/4             | **不是 persona 主因；提示不够具体，底座几何推理也错**                   |
| Skeptical，Teacher 只说“用半圆方程重算” | 按模板拒绝，完整重放旧解，box 1/2                      | 不再拒绝，但误用顶点 `(s,s)`，box 1/4    | **persona 造成抵抗；去掉后仍有数学错误**                                |
| Adaptive，Teacher 已给顶点 `(s/2,s)`    | 沿新表示算出并 `boxed 2/5`                             | 同样算出并 `boxed 2/5`                   | **Teacher 提示足够精确，persona 没有破坏这一步**                        |
| Quick 电费，已要求算 `11520.25÷1.45`    | 继续把除法和原问题问回来                               | 仍复述两个问题，并 box 错误的 7900       | **删 suffix 不恢复：history 惯性 + 算术错误；Teacher 也没有换回验策略** |

按系统要求的 boxed answer 计，这六个刻意挑选的诊断 prefix 从 persona-on 的 **1/6** 提高到 persona-off 的 **3/6**。但这不是总体成功率估计：样本是为区分机制而选的。若只看数学内容，Quick 面积比在 persona-on 时其实已经算出 `2/5`，所以去 persona 带来的“新数学恢复”主要是 Independent 一例；Quick 另一例恢复的是提交格式。

这组 replay 把原因拆成了三类：

1. **persona 本身会破坏表现。** Quick 被迫继续提问，Independent 被迫维持两段模板，Skeptical 被迫拒绝；删除本轮 suffix 后，这些固定话术立即消失。
1. **persona 不是全部原因。** Receptive 和 Skeptical 去掉 suffix 后仍把半圆几何算错；Quick 电费仍得到 7900。这里是底座 Student 的推理/算术问题，或 Teacher 的提示还不够定位错误。
1. **历史会保留 persona 惯性。** Quick 电费即使本轮没有 persona system instruction，仍模仿前六轮的“两句提问”形式。因此“只删当前 suffix”是严格控制，但会低估 persona 对此前 history 的累积污染。

## 7. 结合群体结果后的判断

无 persona 的 historical baseline/pre 就是“标准 Student prompt”对照：Student 模型、API 参数、Teacher 初始 LoRA、reward 和 pre 开关分别与对应 persona run 相同，功能配置差异就是 persona prompt pool。下面全部取相同 update 的 eval 110；但两边 Teacher 是分别训练出来的，所以差值包含 **persona 参与训练 + 前面多轮 history + 当前 suffix** 的总效应，不是纯 test-time suffix 效应。

| Persona     | no-pre：persona 版（相对标准 31.44%） | pre：persona 版（相对标准 47.92%） | 单轮去 persona replay                    |
| ----------- | ------------------------------------: | ---------------------------------: | ---------------------------------------- |
| Quick       |                    1.14%（−30.30 pp） |                 3.79%（−44.13 pp） | 一例恢复正确提交；另一例仍算错且继续提问 |
| Independent |                    29.36%（−2.08 pp） |                37.50%（−10.42 pp） | 所查 prefix 从错误 1 恢复到正确 2/5      |
| Receptive   |                    29.55%（−1.89 pp） |                 49.62%（+1.70 pp） | 去不去都得到同一个错误 1/4               |
| Skeptical   |                   18.56%（−12.88 pp） |                25.00%（−22.92 pp） | 拒绝话术消失，但数学仍错                 |
| Adaptive    |                    25.95%（−5.49 pp） |                 40.34%（−7.58 pp） | 去不去都正确 2/5                         |

这个对应关系是**部分自洽，不是完美一一对应**：

- Receptive 最接近标准 pre（甚至高 1.70 pp），单轮 replay 也完全不受 suffix 去留影响；它的瓶颈更像底座几何推理与提示精度。
- Quick 离标准版最远，replay 又直接观察到提问模板造成不 box；这里 persona 损害证据最强。Skeptical 也远低于标准版，去 suffix 后拒绝消失，说明 instruction 确实制造额外阻力，但一次删除不足以修复数学错误。
- Adaptive 距标准版只有中等差距，所查 replay 两边都正确；这与 persona 相对温和一致。
- Independent 是不能过度简化的反例：no-pre 总体只比标准低 2.08 pp，但特定 prefix 上 persona 明确阻止纠正；到 pre 时总体差扩大到 10.42 pp。群体平均接近不代表每道题都没有 persona harm。

到 eval 170，标准 pre 为 50.57%；Receptive 49.05% 仍最接近，Adaptive 39.77%、Independent 38.45% 居中，Skeptical 28.03% 和 Quick 3.03% 最远，排序没有改变。Quick 约占 20% 的训练样本，并没有少抽到，但训练条件成功率仅 2.19%。

Teacher 已经会按**最新数学错误**调整提示：模糊要求无效后给具体坐标，Student 换表示后沿该表示纠正。但它还不会稳定地按**交互障碍**变招：面对 Quick 仍持续问问题，面对 Skeptical 有时给可核验证据、有时逐步泄题。最准确的结论是：**Teacher 学会了通用的“定位错误—给下一步”，persona 会在部分轨迹中压过推理与提交；但即使拿掉 persona，底座数学错误和 Teacher 提示不足仍解释了剩余失败。**

证据边界：这是六个代表性 prefix、单一原始 seed 的机制实验，不是全 test set 消融。要量化 persona 对总体性能的净损害，应对分层随机抽取的更多 prefix 批量做同样的 paired replay。
