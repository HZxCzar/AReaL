# Models share one OpenAI-compatible endpoint, routed by the model field.
SIM_QWEN_MODEL=qwen3-1.7b
SIM_GEMMA_MODEL=gemma-3-1b-it
SIM_LLAMA_MODEL=llama-3.1-8b-instruct
SIM_PHI_MODEL=phi-4-mini-instruct
STUDENT_BASE_URL=
STUDENT_API_KEY=
# Teacher and answer/leak judge: use the same deployments as the other arms.
SIM_TEACHER_MODEL=
OPENAI_BASE_URL=
OPENAI_API_KEY=
SIM_TEACHER_PRICES=
SIM_AUX_MODEL=
AUX_BASE_URL=
AUX_API_KEY=
# Absolute path to the saved 50-ID manifest from ours (preferred to resampling).
SIM_QUESTION_IDS=
# Optional deployment-specific local dataset and tokenizer paths.
# TUTOR_DATASET=
# TUTOR_TOKENIZER=
