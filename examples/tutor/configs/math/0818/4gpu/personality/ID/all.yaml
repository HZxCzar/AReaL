# the joint in-domain policy at 4gpu. Composition only: what the arm does comes from
# base/personality/ID/all.yaml, how many GPUs it gets from alloc.yaml.
defaults:
  - ../../../base/personality/ID/all@_global_
  - ../../alloc@_global_
  - _self_

trial_name: 0818-personality-all-4gpu

stats_logger:
  wandb:
    group: 0818-personality-4gpu
