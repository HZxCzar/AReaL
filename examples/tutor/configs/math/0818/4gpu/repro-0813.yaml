# repro-0813 at 4 GPUs. Composition only: what the arm does comes from
# base/repro-0813.yaml, how many GPUs it gets from alloc.yaml.
defaults:
  - ../base/repro-0813@_global_
  - alloc@_global_
  - _self_

trial_name: 0818-repro-0813-4gpu

stats_logger:
  wandb:
    group: 0818-4gpu
