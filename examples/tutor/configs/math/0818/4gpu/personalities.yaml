# personalities at 4 GPUs. Composition only: what the arm does comes from
# base/personalities.yaml, how many GPUs it gets from alloc.yaml.
defaults:
  - ../base/personalities@_global_
  - alloc@_global_
  - _self_

trial_name: 0818-personalities-4gpu

stats_logger:
  wandb:
    group: 0818-4gpu
