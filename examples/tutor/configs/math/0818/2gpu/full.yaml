# full-students at 2 GPUs. Composition only: what the arm does comes from
# base/full-students.yaml, how many GPUs it gets from alloc.yaml.
defaults:
  - ../base/full@_global_
  - alloc@_global_
  - _self_

trial_name: 0818-full-2gpu

stats_logger:
  wandb:
    group: 0818-2gpu
