# the surface personality at 2gpu. Composition only: what the arm does comes from
# base/personality/ID/surface.yaml, how many GPUs it gets from alloc.yaml.
defaults:
  - ../../../base/personality/ID/surface@_global_
  - ../../alloc@_global_
  - _self_

trial_name: 0818-personality-surface-2gpu

stats_logger:
  wandb:
    group: 0818-personality-2gpu
