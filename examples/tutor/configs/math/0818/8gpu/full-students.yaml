# full-students at 8 GPUs. Composition only: what the arm does comes from
# base/full-students.yaml, how many GPUs it gets from alloc.yaml.
defaults:
  - ../base/full-students@_global_
  - alloc@_global_
  - _self_

trial_name: 0818-full-students-8gpu

stats_logger:
  wandb:
    group: 0818-8gpu
