# the error_focused personality at 8gpu. Composition only: what the arm does comes from
# base/personality/OOD/error_focused.yaml, how many GPUs it gets from alloc.yaml.
defaults:
  - ../../../base/personality/OOD/error_focused@_global_
  - ../../alloc@_global_
  - _self_

trial_name: 0818-personality-error-focused-8gpu

stats_logger:
  wandb:
    group: 0818-personality-8gpu
