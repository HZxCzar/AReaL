# text/teacher-fade at 4 GPUs. Composition only: what the arm does comes from
# base/life-t10-r8-single-student/text-teacher-fade.yaml, how many GPUs it gets from
# alloc.yaml.
defaults:
  - ../../../base/single/text-teacher-fade@_global_
  - ../../alloc@_global_
  - _self_

trial_name: 0818-text-teacher-fade-8gpu

stats_logger:
  wandb:
    group: 0818-single-student-8gpu
